# Copia esse arquivo pra .env e preenche os valores.

# URL da creative-machine (envia o scrape pra cá)
CM_VERCEL_URL=https://poli-indol-phi.vercel.app

# Token gerado pelo admin pra você (cmtok_...)
# Pede pro admin gerar via:
#   curl -X POST $CM_VERCEL_URL/api/admin/api-tokens \
#     -H "Authorization: Bearer <CRON_SECRET>" \
#     -H "Content-Type: application/json" \
#     -d '{"user_label": "seu-nome"}'
CM_TOKEN=cmtok_29337ba6af2ceb1ba5783a25b9fd4004
